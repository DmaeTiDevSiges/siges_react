# Planejamento: Etapa "Identificar Alertas em Aberto" no Formulário de OS

## 1. Visão Geral

Adicionar uma nova etapa intermediária no wizard de criação de Ordem de Serviço (OS) para identificar e vincular alertas de ativos em aberto que são relevantes para a OS sendo criada.

**Objetivo:** Conectar alertas existentes no sistema (que indicam problemas em equipamentos) com as OSs criadas para resolvê-los, criando um histórico de rastreabilidade entre alertas e ordens de serviço.

---

## 2. Fluxo Atual vs Proposto

### Fluxo Atual (2 etapas)

```
┌─────────────────┐     ┌─────────────────┐
│  Step 1          │     │  Step 2          │
│  Dados da OS     │ ──► │  Evidências      │ ──► Salvar
│  (campos obrig.) │     │  (fotos)         │
└─────────────────┘     └─────────────────┘
```

### Fluxo Proposto (3 etapas)

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Step 1          │     │  Step 2          │     │  Step 3          │
│  Dados da OS     │ ──► │  Alertas em      │ ──► │  Evidências      │ ──► Salvar
│  (campos obrig.) │     │  Aberto (opc.)   │     │  (fotos)         │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

---

## 3. Critérios de Busca de Alertas

A busca de alertas será realizada automaticamente quando o usuário selecionar **Setor > Posição** e **Tipo OS** no Step 1.

### ⚠️ Regra: Mesmo setor, desconsiderando posição

O campo `unitAssetTagId` selecionado no form aponta para um registro em `cfg_units_assets_tags`, que contém:
- `asset_tag_id` → **Setor** (FK para `cfg_assets_tags.id`)
- `asset_tag_sub_id` → **Posição** (FK para `cfg_assets_tags_subs.id`)

Para buscar alertas de **mesmo setor, desconsiderando posição**, devemos:
1. Buscar `cfg_units_assets_tags.asset_tag_id` e `unit_id` a partir do `unitAssetTagId` selecionado → obtém o **sectorId** e o **unitId**
2. Filtrar ativos por `assets.tag_id = sectorId` E `assets.unit_id = unitId` (campos diretos na tabela `assets`, sem usar `unit_asset_tag_id`)

**Não** filtrar por `assets.unit_asset_tag_id` porque isso exigiria mesma posição também.

### Critérios de Filtragem

| Critério | Campo Tabela | Operador | Valor | Nota |
|----------|--------------|----------|-------|------|
| Status Aberto | `assets_alerts.is_done` | = | `false` | Alerta deve estar aberto |
| Não Deletado | `assets_alerts.is_deleted` | = | `false` | Excluir alertas deletados |
| Mesmo Tipo OS | `assets_alerts.o_type_id` | = | `formData.orderTypeId` | Tipo do alerta = Tipo da OS |
| Mesmo Setor | `assets.tag_id` | = | `sectorId` | **Desconsiderar posição** |
| Mesma Unidade | `assets.unit_id` | = | `unitId` | Ativos lotados na mesma unidade |

### Fluxo de Busca (Supabase)

```
1. Resolver o setor E a unidade a partir do unitAssetTagId selecionado:
   SELECT asset_tag_id, unit_id FROM cfg_units_assets_tags 
   WHERE id = {unitAssetTagId}
   → sectorId = result.asset_tag_id
   → unitId = result.unit_id

2. Buscar IDs dos ativos do mesmo setor E mesma unidade (desconsiderando posição):
   SELECT id FROM assets 
   WHERE tag_id = {sectorId}
     AND unit_id = {unitId}
     AND is_deleted = false

3. Buscar alertas abertos desses ativos com mesmo tipo de OS:
   SELECT *, cfg_orders_types(...), cfg_orders_priorities(...)
   FROM assets_alerts
   WHERE asset_id IN ({assetIds})
     AND o_type_id = {orderTypeId}
     AND is_done = false
     AND is_deleted = false
   ORDER BY created_at DESC

4. Enriquecer com dados do ativo (v_assets):
   - code, description, status_code, status_color
   - tag_name, tag_sub_name (setor/posição)
   - unit_description, client_name
```

### Diagrama de Relacionamento

```
cfg_assets_tags (SECTORS)
┌──────────────────┐
│ id          (PK) │◄────── assets.tag_id (filtro por setor)
│ code             │
│ description      │
└──────────────────┘
        ▲
        │ asset_tag_id
        │
cfg_units_assets_tags (JUNCTION)
┌──────────────────────────┐
│ id          (PK)         │ ← unitAssetTagId selecionado no form
│ unit_id      (FK→units)  │ ← RESOLVER ESTE CAMPO para obter unitId
│ asset_tag_id (FK→sector) │ ← RESOLVER ESTE CAMPO para obter sectorId
│ asset_tag_sub_id (FK→pos)│ ← IGNORADO neste filtro
└──────────────────────────┘

assets (ATIVOS)
┌──────────────────────────┐
│ id          (PK)         │
│ tag_id      (FK→sector)  │ ← FILTRO: mesmo setor (sectorId)
│ unit_id     (FK→units)   │ ← FILTRO: mesma unidade (unitId)
│ is_deleted  (bool)       │ ← FILTRO: false
└──────────────────────────┘
```

---

## 4. Estrutura de Dados

### 4.1 Tabela Existente: `assets_alerts`

```sql
CREATE TABLE public.assets_alerts (
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    asset_id bigint,                    -- FK → assets.id
    o_type_id bigint,                   -- FK → cfg_orders_types.id (tipo OS sugerida)
    priority_id bigint,                 -- FK → cfg_orders_priorities.id
    description text,                   -- Descrição do alerta
    is_done boolean DEFAULT false,      -- false = aberto, true = resolvido
    ova_id bigint,                      -- FK → v_orders_visits_assets.id (quando resolvido)
    created_user_id bigint,
    created_at timestamp DEFAULT now(),
    updated_user_id bigint,
    updated_at timestamp,
    is_deleted boolean DEFAULT false,
    deleted_user_id bigint,
    deleted_at timestamp
);
```

### 4.2 Nova Tabela: `orders_alerts`

Tabela de junção para vincular OSs com Alertas (N:N).

```sql
CREATE TABLE IF NOT EXISTS public.orders_alerts (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    order_id bigint NOT NULL,           -- FK → orders.id
    alert_id bigint NOT NULL,           -- FK → assets_alerts.id
    created_at timestamp DEFAULT now(),
    created_user_id bigint,
    CONSTRAINT fk_orders_alerts_order 
        FOREIGN KEY (order_id) 
        REFERENCES public.orders(id) 
        ON DELETE CASCADE,
    CONSTRAINT fk_orders_alerts_alert 
        FOREIGN KEY (alert_id) 
        REFERENCES public.assets_alerts(id) 
        ON DELETE CASCADE,
    CONSTRAINT uq_orders_alerts_order_alert 
        UNIQUE (order_id, alert_id)
);

CREATE INDEX IF NOT EXISTS idx_orders_alerts_order_id 
    ON public.orders_alerts(order_id);
CREATE INDEX IF NOT EXISTS idx_orders_alerts_alert_id 
    ON public.orders_alerts(alert_id);
```

**Justificativa das constraints:**
- `ON DELETE CASCADE`: Se uma OS for deletada, os vínculos são removidos (não os alertas)
- `UNIQUE (order_id, alert_id)`: Impede vínculos duplicados entre mesma OS e alerta

### 4.3 Interface TypeScript Atualizada: `AssetAlert`

```typescript
export interface AssetAlert {
    // Campos existentes
    id: string;
    assetId: string;
    oTypeId?: string;
    priorityId?: string;
    description?: string;
    isDone: boolean;
    ovaId?: string;
    createdUserId?: string;
    createdAt?: string;
    updatedUserId?: string;
    updatedAt?: string;
    isDeleted: boolean;
    deletedUserId?: string;
    deletedAt?: string;

    // UI Helpers existentes
    orderTypeName?: string;
    priorityName?: string;
    priorityColor?: string;
    resolvedAt?: string;
    assetDescription?: string;
    assetCode?: string;
    assetStatusName?: string;
    assetStatusColor?: string;
    clientName?: string;
    unitDescription?: string;
    tagName?: string;
    tagSubName?: string;

    // NOVOS CAMPOS
    orderId?: string;      // ID da OS vinculada (para exibir orderMask)
    orderMask?: string;    // orderMask da OS vinculada (ex: "123.0.2026")
}
```

---

## 5. Alterações nos Serviços

### 5.1 `services/assets/assetsService.ts`

Nova função para buscar alertas por setor (desconsiderando posição) e tipo:

```typescript
/**
 * Busca alertas abertos por setor (desconsiderando posição), mesma unidade e tipo de OS
 * Usado na etapa de seleção de alertas do wizard de criação de OS
 * 
 * @param unitAssetTagId - ID do registro em cfg_units_assets_tags (selecionado no form)
 * @param orderTypeId - ID do tipo de OS selecionado
 */
async getOpenAlertsBySectorAndType(
    unitAssetTagId: string, 
    orderTypeId: string
): Promise<AssetAlert[]> {
    // 1. Resolver o setor E a unidade a partir do unitAssetTagId
    //    (unitAssetTagId → cfg_units_assets_tags.asset_tag_id = sectorId, unit_id = unitId)
    const { data: tagData, error: tagError } = await supabase
        .from('cfg_units_assets_tags')
        .select('asset_tag_id, unit_id')
        .eq('id', parseInt(unitAssetTagId))
        .single();

    if (tagError || !tagData?.asset_tag_id || !tagData?.unit_id) {
        return [];
    }

    const sectorId = tagData.asset_tag_id;
    const unitId = tagData.unit_id;

    // 2. Buscar IDs dos ativos do mesmo setor E mesma unidade (desconsiderando posição)
    //    assets.tag_id = sectorId AND assets.unit_id = unitId
    const { data: assets, error: assetsError } = await supabase
        .from('assets')
        .select('id')
        .eq('tag_id', sectorId)
        .eq('unit_id', unitId)
        .eq('is_deleted', false);

    if (assetsError || !assets || assets.length === 0) {
        return [];
    }

    const assetIds = assets.map(a => a.id);

    // 3. Buscar alertas abertos desses ativos com mesmo tipo de OS
    //    JOIN com orders_alerts + orders para saber se já está vinculado a outra OS
    const { data: alertsData, error: alertsError } = await supabase
        .from('assets_alerts')
        .select(`
            *,
            cfg_orders_types ( description ),
            cfg_orders_priorities ( description, color ),
            orders_alerts!left (
                order_id,
                orders ( id, order_mask )
            )
        `)
        .in('asset_id', assetIds)
        .eq('o_type_id', parseInt(orderTypeId))
        .eq('is_done', false)
        .eq('is_deleted', false)
        .order('created_at', { ascending: false });

    if (alertsError || !alertsData || alertsData.length === 0) {
        return [];
    }

    // 4. Enriquecer com dados do ativo (v_assets)
    const enrichedAssetIds = [...new Set(alertsData.map((d: any) => d.asset_id).filter(Boolean))];
    
    let assetsMap = new Map<string, any>();
    if (enrichedAssetIds.length > 0) {
        const { data: assetsDataList } = await supabase
            .from('v_assets')
            .select('*')
            .in('id', enrichedAssetIds);

        if (assetsDataList) {
            assetsMap = new Map(assetsDataList.map((a: any) => [a.id.toString(), a]));
            
            // Buscar nomes dos clientes
            const unitIds = [...new Set(assetsDataList.map((a: any) => a.unit_id).filter(Boolean))];
            if (unitIds.length > 0) {
                const { data: unitsData } = await supabase
                    .from('v_units')
                    .select('id, client_name')
                    .in('id', unitIds);
                    
                if (unitsData) {
                    unitsData.forEach((u: any) => {
                        for (const a of assetsDataList) {
                            if (a.unit_id?.toString() === u.id?.toString()) {
                                a.injected_client_name = u.client_name;
                            }
                        }
                    });
                }
            }
        }
    }

    // 5. Mapear resultado
    return alertsData.map((d: any) => {
        const assetIdStr = d.asset_id?.toString();
        const asset = assetIdStr ? assetsMap.get(assetIdStr) : null;

        // Extrair info da OS vinculada (se existir)
        const linkedOrder = d.orders_alerts?.[0]?.orders;
        const orderId = linkedOrder?.id?.toString() || null;
        const orderMask = linkedOrder?.order_mask || null;

        return {
            id: d.id.toString(),
            assetId: d.asset_id?.toString(),
            assetDescription: asset?.description,
            assetCode: asset?.code,
            assetStatusName: asset?.status_code || '',
            assetStatusColor: asset?.status_color || '',
            clientName: asset?.injected_client_name || asset?.client_name || asset?.client_description || '',
            unitDescription: asset?.unit_description_full || asset?.unit_description || asset?.description_full || '',
            tagName: asset?.tag_name || asset?.tag_description || asset?.asset_tag_description || '',
            tagSubName: asset?.tag_sub_name || asset?.tag_sub_description || asset?.asset_tag_sub_description || '',
            oTypeId: d.o_type_id?.toString(),
            orderTypeName: d.cfg_orders_types?.description,
            priorityId: d.priority_id?.toString(),
            priorityName: d.cfg_orders_priorities?.description,
            priorityColor: d.cfg_orders_priorities?.color,
            description: d.description,
            isDone: d.is_done,
            ovaId: d.ova_id?.toString(),
            createdAt: d.created_at,
            createdUserId: d.created_user_id?.toString(),
            orderId: orderId,
            orderMask: orderMask
        };
    }) as AssetAlert[];
}
```

### 5.2 `services/orders/ordersService.ts`

Novas funções para gerenciar vínculos OS-Alertas:

```typescript
/**
 * Vincula alertas a uma OS (insere na tabela orders_alerts)
 */
async linkAlertsToOrder(orderId: string, alertIds: string[]): Promise<void> {
    if (!alertIds || alertIds.length === 0) return;

    const { usersService } = await import('../users/usersService');
    const currentUser = await usersService.getCurrentUser();

    const records = alertIds.map(alertId => ({
        order_id: parseInt(orderId),
        alert_id: parseInt(alertId),
        created_user_id: currentUser ? parseInt(currentUser.id) : null,
        created_at: getBrazilTimestamp()
    }));

    const { error } = await supabase
        .from('orders_alerts')
        .insert(records);

    if (error) {
        console.error('Error linking alerts to order:', error);
        throw error;
    }
}

/**
 * Remove vínculo de um alerta com uma OS
 */
async unlinkAlertFromOrder(orderId: string, alertId: string): Promise<void> {
    const { error } = await supabase
        .from('orders_alerts')
        .delete()
        .eq('order_id', parseInt(orderId))
        .eq('alert_id', parseInt(alertId));

    if (error) {
        console.error('Error unlinking alert from order:', error);
        throw error;
    }
}

/**
 * Busca alertas vinculados a uma OS
 */
async getAlertsByOrderId(orderId: string): Promise<AssetAlert[]> {
    const { data, error } = await supabase
        .from('orders_alerts')
        .select('alert_id')
        .eq('order_id', parseInt(orderId));

    if (error || !data || data.length === 0) {
        return [];
    }

    const alertIds = data.map(r => r.alert_id);
    
    // Usar a função getAssetAlerts para cada alerta ou criar uma query otimizada
    // ... (buscar dados completos dos alertas)
}
```

### 5.3 `services/dataService.ts` (Proxy)

Adicionar os proxies correspondentes:

```typescript
// Proxy para busca de alertas por setor (desconsiderando posição) e tipo
async getOpenAlertsBySectorAndType(
    unitAssetTagId: string, 
    orderTypeId: string
): Promise<AssetAlert[]> {
    return assetsService.getOpenAlertsBySectorAndType.apply(
        assetsService, 
        arguments as any
    );
}

// Proxy para vínculo OS-Alertas
async linkAlertsToOrder(orderId: string, alertIds: string[]): Promise<void> {
    return ordersService.linkAlertsToOrder.apply(
        ordersService, 
        arguments as any
    );
}

// Proxy para desvincular alerta de OS
async unlinkAlertFromOrder(orderId: string, alertId: string): Promise<void> {
    return ordersService.unlinkAlertFromOrder.apply(
        ordersService, 
        arguments as any
    );
}

// Proxy para buscar alertas de uma OS
async getAlertsByOrderId(orderId: string): Promise<AssetAlert[]> {
    return ordersService.getAlertsByOrderId.apply(
        ordersService, 
        arguments as any
    );
}
```

---

## 6. Alterações no Frontend

### 6.1 Componentes a Criar

#### `OrderAlertCard.tsx` (Novo Componente)

Card de seleção de alerta para o wizard de criação de OS.

```typescript
interface OrderAlertCardProps {
    alert: AssetAlert;
    isSelected: boolean;
    onToggle: () => void;
    disabled?: boolean;
}
```

**Estrutura do Card:**

```
┌─────────────────────────────────────────────────────────────┐
│ [✓] COD-001                                    [Aberto]    │
│     Descricao do equipamento                               │
│                                                             │
│ ┌─────────┐ ┌─────────┐                                    │
│ │CRITICO  │ │MANUTENCAO│                                   │
│ └─────────┘ └─────────┘                                    │
│                                                             │
│ Descricao do alerta...                                     │
│                                                             │
│ OS: 123.0.2026 (se vinculado → badge azul)                │
│                                                             │
│ 25/07/2026 14:30                                           │
└─────────────────────────────────────────────────────────────┘
```

**Elementos visuais:**
- Checkbox no topo-esquerda para seleção (**desabilitado** se alerta já vinculado a outra OS)
- Badge com código do ativo (cor do status)
- Badge "Aberto" (vermelho)
- Badges de prioridade e tipo de OS
- Descrição do alerta
- Se tiver OS vinculada: badge azul com `orderMask` + checkbox desabilitado
- Data de criação

**Regra de seleção:**
- Se `alert.orderId` existir → alerta já vinculado a outra OS → checkbox desabilitado + tooltip "Já vinculado à OS {orderMask}"
- Se `alert.orderId` for nulo → alerta disponível para vinculação

### 6.2 Alterações em `OrderRequestForm.tsx`

#### Novos States

```typescript
// Alertas
const [availableAlerts, setAvailableAlerts] = useState<AssetAlert[]>([]);
const [selectedAlertIds, setSelectedAlertIds] = useState<string[]>([]);
const [loadingAlerts, setLoadingAlerts] = useState(false);
```

#### Efeito para Buscar Alertas

```typescript
useEffect(() => {
    const loadAlerts = async () => {
        // Requer setor E tipo OS preenchidos
        if (formData.unitAssetTagId && formData.orderTypeId) {
            setLoadingAlerts(true);
            try {
                // A função internamente resolve unitAssetTagId → sectorId (asset_tag_id) + unitId (unit_id)
                // e busca ativos com assets.tag_id = sectorId AND assets.unit_id = unitId (desconsiderando posição)
                const alerts = await dataService.getOpenAlertsBySectorAndType(
                    formData.unitAssetTagId,
                    formData.orderTypeId
                );
                setAvailableAlerts(alerts);
            } catch (err) {
                console.error("Error loading alerts", err);
                setAvailableAlerts([]);
            } finally {
                setLoadingAlerts(false);
            }
        } else {
            setAvailableAlerts([]);
            setSelectedAlertIds([]);
        }
    };
    
    loadAlerts();
}, [formData.unitAssetTagId, formData.orderTypeId]);
```

#### Atualização da Navegação

```typescript
const handleNext = () => {
    if (step === 1 && isStep1Valid) {
        setStep(2);  // Ir para Step 2 (Alertas)
    } else if (step === 2) {
        setStep(3);  // Ir para Step 3 (Evidências) - alertas são opcionais
    } else if (step === 3) {
        handleSubmit();  // Enviar
    } else {
        toast.error("Preencha os campos obrigatórios");
    }
};

const handlePrev = () => {
    if (step > 1) setStep(step - 1);
    else onBack();
};
```

#### Função de Toggle de Seleção

```typescript
const toggleAlertSelection = (alertId: string) => {
    // Verificar se o alerta já está vinculado a outra OS
    const alert = availableAlerts.find(a => a.id === alertId);
    if (alert?.orderId) {
        toast.warning(`Este alerta já está vinculado à OS ${alert.orderMask}`);
        return;
    }
    
    setSelectedAlertIds(prev => 
        prev.includes(alertId)
            ? prev.filter(id => id !== alertId)
            : [...prev, alertId]
    );
};
```

#### Atualização do Submit

```typescript
const handleSubmit = async (): Promise<boolean> => {
    // ... validação existente ...

    try {
        // ... criar OS (código existente) ...

        // Vincular alertas selecionados
        if (selectedAlertIds.length > 0 && resultOrder.id) {
            await dataService.linkAlertsToOrder(
                resultOrder.id, 
                selectedAlertIds
            );
        }

        // ... upload de imagens (código existente) ...
    } catch (error) {
        // ...
    }
};
```

#### Atualização do Stepper Visual

```tsx
{/* Stepper - 3 steps */}
{!hideFooter && (
    <div className="flex items-center gap-2 mb-2">
        <div className={`w-7 h-7 rounded-md flex items-center justify-center text-[11px] font-black transition-colors ${
            step >= 1 ? 'bg-blue-600 text-white' : 'bg-white/20 text-white/50'
        }`}>1</div>
        
        <div className={`w-5 h-0.5 rounded-full transition-colors ${
            step >= 2 ? 'bg-blue-600' : 'bg-white/20'
        }`} />
        
        <div className={`w-7 h-7 rounded-md flex items-center justify-center text-[11px] font-black transition-colors ${
            step >= 2 ? 'bg-blue-600 text-white' : 'bg-white/20 text-white/50'
        }`}>2</div>
        
        <div className={`w-5 h-0.5 rounded-full transition-colors ${
            step >= 3 ? 'bg-blue-600' : 'bg-white/20'
        }`} />
        
        <div className={`w-7 h-7 rounded-md flex items-center justify-center text-[11px] font-black transition-colors ${
            step >= 3 ? 'bg-blue-600 text-white' : 'bg-white/20 text-white/50'
        }`}>3</div>
    </div>
)}
```

#### Título Dinâmico do Step

```typescript
const getStepTitle = () => {
    if (isEdit) {
        return step === 1 ? "Editar OS" : 
               step === 2 ? "Alertas em Aberto" : "Evidências";
    }
    return step === 1 ? "Dados da OS" : 
           step === 2 ? "Alertas em Aberto" : "Evidências";
};
```

#### Renderização do Step 2 (Alertas)

```tsx
{(step === 2 && !hideFooter) && (
    <section className="space-y-4">
        <div className="flex items-center justify-between">
            <div>
                <h3 className="text-sm font-bold text-slate-800 dark:text-white">
                    Alertas em Aberto
                </h3>
                <p className="text-xs text-slate-500 dark:text-slate-400 mt-1">
                    {loadingAlerts 
                        ? 'Buscando alertas...'
                        : availableAlerts.length === 0
                            ? 'Nenhum alerta encontrado para este setor e tipo de OS'
                            : `${availableAlerts.length} alerta(s) encontrado(s)`
                    }
                </p>
            </div>
            {selectedAlertIds.length > 0 && (
                <span className="text-xs font-bold text-blue-600 dark:text-blue-400">
                    {selectedAlertIds.length} selecionado(s)
                </span>
            )}
        </div>

        {loadingAlerts ? (
            <div className="flex items-center justify-center py-12">
                <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600"></div>
            </div>
        ) : availableAlerts.length === 0 ? (
            <div className="text-center py-12">
                <span className="material-symbols-outlined text-4xl text-slate-300 dark:text-slate-600 mb-2">
                    notifications_off
                </span>
                <p className="text-sm text-slate-500 dark:text-slate-400">
                    Nenhum alerta em aberto para o setor e tipo de OS selecionados
                </p>
            </div>
        ) : (
            <div className="space-y-3">
                {availableAlerts.map(alert => (
                    <OrderAlertCard
                        key={alert.id}
                        alert={alert}
                        isSelected={selectedAlertIds.includes(alert.id)}
                        onToggle={() => toggleAlertSelection(alert.id)}
                    />
                ))}
            </div>
        )}

        <div className="flex gap-4 py-4 pt-2">
            <Button
                variant="secondary"
                onClick={handlePrev}
                className="flex-1 text-slate-500 hover:bg-slate-300 dark:text-slate-400 dark:hover:text-white dark:hover:bg-slate-800 shadow-sm min-h-[52px] rounded-2xl"
                disabled={isLoading}
            >
                Voltar
            </Button>

            <Button
                onClick={handleNext}
                className="flex-1 min-h-[52px] rounded-2xl"
            >
                Próximo
            </Button>
        </div>
    </section>
)}
```

---

## 7. Fluxo Completo

### 7.1 Criação de OS com Alertas

```
┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 1: DADOS DA OS                                                    │
├─────────────────────────────────────────────────────────────────────────┤
│ Usuário preenche:                                                      │
│ - Serviços a serem executados                                          │
│ - Cliente → Unidade → Setor > Posição                                  │
│ - Tipo OS → Sub-Tipo OS                                                │
│ - Prioridade → Finalidade → Contrato → Plano                           │
│                                                                         │
│ Ao selecionar Setor + Tipo OS:                                         │
│ └── Buscar alertas abertos no setor com mesmo tipo                     │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 2: ALERTAS EM ABERTO                                              │
├─────────────────────────────────────────────────────────────────────────┤
│ Exibe lista de alertas encontrados (ou mensagem "nenhum encontrado")   │
│                                                                         │
│ Cada card mostra:                                                      │
│ - Código do ativo + Status                                             │
│ - Descrição do equipamento                                             │
│ - Badge Prioridade + Badge Tipo OS                                     │
│ - Descrição do alerta                                                  │
│ - Se tiver OS vinculada: badge com orderMask                           │
│ - Checkbox para seleção                                                │
│                                                                         │
│ Usuário pode:                                                          │
│ - Selecionar alertas para vincular (opcional)                          │
│ - Avançar sem selecionar nenhum                                        │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 3: EVIDÊNCIAS                                                     │
├─────────────────────────────────────────────────────────────────────────┤
│ Upload de até 4 fotos (galeria ou câmera)                              │
│ Edição de imagens                                                      │
└─────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│ SUBMIT                                                                 │
├─────────────────────────────────────────────────────────────────────────┤
│ 1. Criar registro na tabela orders                                     │
│ 2. Vincular alertas selecionados na tabela orders_alerts               │
│ 3. Upload das imagens para R2                                          │
│ 4. Atualizar ordem com paths das imagens                               │
└─────────────────────────────────────────────────────────────────────────┘
```

### 7.2 Exibição de orderMask nos Cards

Quando um alerta já estiver vinculado a uma OS (via `orders_alerts`), o campo `orderMask` será exibido no card:

```
┌─────────────────────────────────────────────┐
│ [ ] COD-001                    [Aberto]     │
│     Motor Bomba Hidráulica                  │
│                                             │
│ [CRITICO] [MANUTENCAO]                     │
│                                             │
│ Vazamento de óleo identificado no...        │
│                                             │
│ OS: 123.0.2026                             │  ← Badge azul
│                                             │
│ 25/07/2026 14:30                           │
└─────────────────────────────────────────────┘
```

**Nota:** Se o alerta já tiver OS vinculada, ele aparecerá na lista mas com o checkbox desabilitado (para não vincular o mesmo alerta a duas OSs).

---

## 8. Validações e Regras de Negócio

### 8.1 Regras de Exibição do Step 2

| Condição | Comportamento |
|----------|---------------|
| `unitAssetTagId` não preenchido | Step 2 exibe mensagem "Selecione um setor para ver alertas" |
| `orderTypeId` não preenchido | Step 2 exibe mensagem "Selecione um tipo de OS para ver alertas" |
| Nenhum alerta encontrado | Step 2 exibe "Nenhum alerta em aberto para este setor e tipo" |
| Alertas encontrados | Lista de cards com opção de seleção |
| Alerta já vinculado a outra OS | Card exibido com badge da OS, checkbox desabilitado |

### 8.2 Regras de Seleção

| Regra | Descrição |
|-------|-----------|
| Seleção opcional | Usuário pode avançar sem selecionar nenhum alerta |
| Sem limite de seleção | Pode selecionar vários alertas de uma vez |
| Sem duplicatas | UNIQUE constraint na tabela `orders_alerts` |
| Desvincular | Usuário pode desmarcar alerta antes de enviar |

### 8.3 Regras ao Salvar

| Regra | Descrição |
|-------|-----------|
| Criar OS primeiro | OS deve ser criada antes de vincular alertas |
| Vincular alertas | Inserir registros na tabela `orders_alerts` |
| Rollback em erro | Se falhar ao vincular, a OS já criada não é removida (log de erro) |

---

## 9. Arquivos a Modificar

### 9.1 Arquivos a Criar

| Arquivo | Descrição |
|---------|-----------|
| `views/OrderRequest/OrderAlertCard.tsx` | Card de seleção de alerta |
| `supabase/migrations/XXXX_add_orders_alerts.sql` | Migration da nova tabela |

### 9.2 Arquivos a Modificar

| Arquivo | Alteração |
|---------|-----------|
| `services/assets/assetsService.ts` | Adicionar `getOpenAlertsBySectorAndType()` |
| `services/orders/ordersService.ts` | Adicionar `linkAlertsToOrder()`, `unlinkAlertFromOrder()`, `getAlertsByOrderId()` |
| `services/dataService.ts` | Adicionar proxies das novas funções |
| `types.ts` | Atualizar interface `AssetAlert` com `orderId` e `orderMask` |
| `views/OrderRequest/OrderRequestForm.tsx` | Adicionar Step 2, states, effects, lógica |
| `views/Assets/AssetAlertListItem.tsx` | Opcional: exibir `orderMask` quando disponível |

---

## 10. Notas Importantes sobre Campos de Setor

### Diferença entre `unitAssetTagId` e `tag_id`

| Campo | Tabela | Descrição | Uso no Form |
|-------|--------|-----------|-------------|
| `unitAssetTagId` | `cfg_units_assets_tags.id` | Registro composto (unidade + setor + posição) | Selecionado no dropdown "Setor > Posição" |
| `asset_tag_id` | `cfg_units_assets_tags.asset_tag_id` | FK para o setor (derivado do `unitAssetTagId`) | Usado para filtrar por setor |
| `unit_id` | `cfg_units_assets_tags.unit_id` | FK para a unidade (derivado do `unitAssetTagId`) | Usado para filtrar por unidade |
| `tag_id` | `assets.tag_id` | FK para o setor (armazenado no ativo) | Usado para buscar ativos do mesmo setor |
| `unit_id` | `assets.unit_id` | FK para a unidade (armazenada no ativo) | Usado para buscar ativos da mesma unidade |

**Fluxo de resolução:**
```
Usuário seleciona "Setor > Posição" no form
    ↓
formData.unitAssetTagId = cfg_units_assets_tags.id
    ↓
Busca: SELECT asset_tag_id, unit_id FROM cfg_units_assets_tags WHERE id = {unitAssetTagId}
    ↓
sectorId = asset_tag_id (o setor, sem a posição)
unitId = unit_id (a unidade)
    ↓
Busca alertas: assets.tag_id = {sectorId} AND assets.unit_id = {unitId}
```

### Por que não usar `unitAssetTagId` diretamente?

Se filtrarmos por `assets.unit_asset_tag_id = {unitAssetTagId}`, estaríamos buscando apenas ativos **na mesma posição específica**. O usuário quer buscar alertas de **todo o setor**, independente da posição.

Exemplo:
- Setor A tem Posição 1 e Posição 2
- Usuário seleciona "Setor A > Posicao 1" → `unitAssetTagId = 10`
- Se filtrarmos por `unit_asset_tag_id = 10`, encontramos apenas ativos da Posicao 1
- **Correto:** filtrar por `tag_id = 5` (Setor A), encontrando ativos da Posicao 1 **e** Posicao 2

---

## 11. Questões a Confirmar com o Usuário

### 11.1 Comportamento quando Setor não é informado

O campo `unitAssetTagId` (Setor > Posição) é **opcional** no formulário atual.

**Opção A:** Pular Step 2 automaticamente
- Se o usuário não selecionou setor, avança direto para Step 3
- **Prós:** Fluxo mais rápido
- **Contras:** Usuário pode esquecer de selecionar setor e perder alertas relevantes

**Opção B:** Exibir Step 2 vazia com mensagem
- Exibir "Selecione um setor no Step 1 para ver alertas em aberto"
- **Prós:** Mantém consistência visual, lembra o usuário
- **Contras:** Step adicional sem utilidade

**Recomendação:** Opção B (manter Step 2 sempre visível)

### 11.2 Alertas já vinculados a outra OS

**Opção A:** Exibir card com badge da OS, checkbox desabilitado
- Usuário vê que o alerta já está sendo atendido
- **Prós:** Transparência, evita duplicação
- **Contras:** Mais complexidade no card

**Opção B:** Não exibir alertas já vinculados
- Lista só mostra alertas sem OS vinculada
- **Prós:** Lista mais limpa
- **Contras:** Usuário não sabe que o alerta já existe

**Recomendação:** Opção A (exibir com badge e desabilitado)

### 11.3 Edição de OS existente

Ao editar uma OS que já possui alertas vinculados:

**Opção A:** Carregar vínculos existentes e permitir adicionar/remover
- **Prós:** Flexibilidade total
- **Contras:** Mais complexidade

**Opção B:** Somente exibir os já vinculados (somente leitura)
- **Prós:** Mais simples
- **Contras:** Não permite alterações

**Recomendação:** Opção A (permitir edição)

### 11.4 Deletar OS

Ao deletar OS, os alertas vinculados devem:

**Opção A:** Ficar desvinculados (alertas permanecem no sistema)
- Usar `ON DELETE CASCADE` na tabela `orders_alerts`
- Alertas voltam a ficar "em aberto"
- **Prós:** Alertas não se perdem
- **Contras:** Pode criar confusão se alerta foi resolvido junto com a OS

**Opção B:** Marcar alertas como resolvidos
- Ao deletar OS, marcar alertas vinculados como `is_done = true`
- **Prós:** Mantém consistência
- **Contras:** Pode resolver alertas que não foram realmente atendidos

**Recomendação:** Opção A (desvincular, manter alertas)

---

## 12. Estimativa de Esforço

| Componente | Prioridade | Complexidade |
|------------|-----------|--------------|
| Tabela `orders_alerts` + migration SQL | Alta | Baixa |
| Service `getOpenAlertsBySectorAndType` | Alta | Média |
| Service `linkAlertsToOrder` | Alta | Baixa |
| Service `getAlertsByOrderId` | Média | Baixa |
| `OrderAlertCard.tsx` | Alta | Média |
| `OrderRequestForm.tsx` — Step 2 | Alta | Alta |
| Atualização de `types.ts` | Alta | Baixa |
| Proxy em `dataService.ts` | Alta | Baixa |
| Exibição de `orderMask` em cards | Média | Baixa |
| Validações e edge cases | Média | Média |

---

## 13. Próximos Passos

1. Confirmar as questões da Seção 11 com o usuário
2. Criar migration SQL da tabela `orders_alerts`
3. Adicionar funções no `assetsService.ts`
4. Adicionar funções no `ordersService.ts`
5. Atualizar `dataService.ts` com proxies
6. Atualizar `types.ts` com novos campos
7. Criar componente `OrderAlertCard.tsx`
8. Modificar `OrderRequestForm.tsx` para 3 steps
9. Testar fluxo completo
10. Opcional: Atualizar `AssetAlertListItem.tsx` para exibir `orderMask`
