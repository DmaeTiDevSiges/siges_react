# 📋 Documento de Implantação - Sistema de Avaliação de Serviços

## 📅 Data: 07/08/2026

---

## 1. Resumo do Projeto

Sistema de avaliação de serviços prestados durante visitas técnicas, permitindo que o aprovador avalie requisitos pré-definidos vinculados a contratos, com pontuação negativa para requisitos não cumpridos.

---

## 2. Arquivos Criados/Modificados

### 2.1 Banco de Dados

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `dev/supabase/migrations/20260806_create_evaluation_system.sql` | **CRIADO** | Migração SQL completa |

**Tabelas criadas:**
- `cfg_evaluation_requirements` — Catálogo de requisitos gerais
- `contracts_evaluation_requirements` — Vinculação contrato × requisito (com peso)
- `orders_visits_evaluations` — Avaliação registrada na visita

**Views criadas:**
- `v_orders_visits_evaluations` — View de avaliações da visita
- `v_contracts_evaluation_requirements` — View de requisitos do contrato

### 2.2 Backend (Services)

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `services/companies/evaluationService.ts` | **CRIADO** | Serviço completo de avaliação |
| `services/dataService.ts` | **MODIFICADO** | Adicionados 12 métodos proxy |

### 2.3 Frontend (Components)

| Arquivo | Tipo | Descrição |
|---------|------|-----------|
| `views/Contracts/Evaluations/ContractEvaluationsTab.tsx` | **CRIADO** | Aba de vinculação no contrato |
| `views/Contracts/Evaluations/EvaluationRequirementsList.tsx` | **CRIADO** | Lista de requisitos gerais |
| `views/Visits/VisitEvaluationPage.tsx` | **CRIADO** | Página dedicada de avaliação |
| `views/Dashboards/DashboardAdminContractsEvaluationsRequirements.tsx` | **CRIADO** | Dashboard de métricas |
| `views/Contracts/Evaluations/index.ts` | **CRIADO** | Exportação dos componentes |

### 2.4 Integração

| Arquivo | Tipo | Alteração |
|---------|------|-----------|
| `types.ts` | **MODIFICADO** | +3 interfaces (EvaluationRequirement, ContractEvaluationRequirement, OrderVisitEvaluation) |
| `views/Contracts/ContractDetails.tsx` | **MODIFICADO** | +aba "Avaliações" |
| `views/OrderRequest/OrderRequestApproveConfirm.tsx` | **MODIFICADO** | +botão "Avaliar Serviços" |
| `App.tsx` | **MODIFICADO** | +2 telas (order-visit-evaluation, dashboard-contracts-evaluations) |
| `components/Sidebar.tsx` | **MODIFICADO** | +navegação para dashboard |
| `components/BottomNav.tsx` | **MODIFICADO** | +navegação para dashboard |
| `components/DashboardTabs.tsx` | **MODIFICADO** | +aba "Avaliações" |

---

## 3. Estrutura de Banco de Dados

### 3.1 Tabela `cfg_evaluation_requirements`

```sql
CREATE TABLE IF NOT EXISTS public.cfg_evaluation_requirements (
    id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    description character varying(255) NOT NULL,
    code character varying(50),
    is_available boolean DEFAULT true,
    is_deleted boolean DEFAULT false,
    created_at timestamp without time zone DEFAULT now(),
    updated_at timestamp without time zone DEFAULT now(),
    CONSTRAINT cfg_evaluation_requirements_pkey PRIMARY KEY (id),
    CONSTRAINT cfg_evaluation_requirements_code_key UNIQUE (code)
);
```

### 3.2 Tabela `contracts_evaluation_requirements`

```sql
CREATE TABLE IF NOT EXISTS public.contracts_evaluation_requirements (
    id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    contract_id bigint NOT NULL,
    evaluation_id bigint NOT NULL,
    weight integer NOT NULL,
    is_available boolean DEFAULT true,
    is_deleted boolean DEFAULT false,
    created_at timestamp without time zone DEFAULT now(),
    CONSTRAINT contracts_evaluation_requirements_pkey PRIMARY KEY (id),
    CONSTRAINT contracts_evaluation_requirements_contract_id_fkey FOREIGN KEY (contract_id)
        REFERENCES public.contracts (id) ON DELETE CASCADE,
    CONSTRAINT contracts_evaluation_requirements_evaluation_id_fkey FOREIGN KEY (evaluation_id)
        REFERENCES public.cfg_evaluation_requirements (id) ON DELETE CASCADE,
    CONSTRAINT contracts_evaluation_requirements_contract_evaluation_unique UNIQUE (contract_id, evaluation_id)
);
```

### 3.3 Tabela `orders_visits_evaluations`

```sql
CREATE TABLE IF NOT EXISTS public.orders_visits_evaluations (
    id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    ov_id bigint NOT NULL,
    contract_evaluation_id bigint NOT NULL,
    was_applied boolean NOT NULL DEFAULT false,
    notes text,
    evaluated_by_user_id bigint NOT NULL,
    evaluated_at timestamp without time zone DEFAULT now(),
    created_at timestamp without time zone DEFAULT now(),
    CONSTRAINT orders_visits_evaluations_pkey PRIMARY KEY (id),
    CONSTRAINT orders_visits_evaluations_ov_id_fkey FOREIGN KEY (ov_id)
        REFERENCES public.orders_visits (id) ON DELETE CASCADE,
    CONSTRAINT orders_visits_evaluations_contract_evaluation_id_fkey FOREIGN KEY (contract_evaluation_id)
        REFERENCES public.contracts_evaluation_requirements (id) ON DELETE CASCADE,
    CONSTRAINT orders_visits_evaluations_evaluated_by_user_id_fkey FOREIGN KEY (evaluated_by_user_id)
        REFERENCES public.users (id) ON DELETE RESTRICT
);
```

---

## 4. Interfaces TypeScript

### 4.1 EvaluationRequirement

```typescript
export interface EvaluationRequirement {
  id: string;
  description: string;
  code?: string;
  isAvailable: boolean;
  isDeleted?: boolean;
  createdAt?: string;
  updatedAt?: string;
}
```

### 4.2 ContractEvaluationRequirement

```typescript
export interface ContractEvaluationRequirement {
  id: string;
  contractId: string;
  evaluationId: string;
  weight: number;
  isAvailable: boolean;
  isDeleted?: boolean;
  createdAt?: string;
  evaluationDescription?: string;
  evaluationCode?: string;
}
```

### 4.3 OrderVisitEvaluation

```typescript
export interface OrderVisitEvaluation {
  id: string;
  ovId: string;
  contractEvaluationId: string;
  wasApplied: boolean;
  notes?: string;
  evaluatedByUserId: string;
  evaluatedAt: string;
  createdAt?: string;
  requirementDescription?: string;
  requirementCode?: string;
  weight?: number;
  evaluatorName?: string;
  contractId?: string;
}
```

---

## 5. Métodos do Serviço

### 5.1 Requisitos Gerais

```typescript
evaluationService.getEvaluationRequirements(): Promise<EvaluationRequirement[]>
evaluationService.createEvaluationRequirement(data: { description: string; code?: string }): Promise<EvaluationRequirement | null>
evaluationService.updateEvaluationRequirement(id: string, data: { description?: string; code?: string; isAvailable?: boolean }): Promise<boolean>
evaluationService.deleteEvaluationRequirement(id: string): Promise<boolean>
```

### 5.2 Vinculação Contrato × Requisito

```typescript
evaluationService.getContractEvaluationRequirements(contractId: string): Promise<ContractEvaluationRequirement[]>
evaluationService.addEvaluationToContract(contractId: string, evaluationId: string, weight: number): Promise<ContractEvaluationRequirement | null>
evaluationService.updateContractEvaluationWeight(id: string, weight: number): Promise<boolean>
evaluationService.removeEvaluationFromContract(id: string): Promise<boolean>
```

### 5.3 Avaliações da Visita

```typescript
evaluationService.getVisitEvaluations(ovId: string): Promise<OrderVisitEvaluation[]>
evaluationService.saveVisitEvaluations(ovId: string, evaluations: { contractEvaluationId: string; wasApplied: boolean; notes?: string }[], userId: string): Promise<boolean>
evaluationService.getVisitTotalScore(ovId: string): Promise<number>
evaluationService.deleteVisitEvaluations(ovId: string): Promise<boolean>
```

---

## 6. Fluxo de Uso

### 6.1 Configuração (Admin)

1. Acessar **Configurações > Contratos**
2. Selecionar um contrato
3. Clicar na aba **"Avaliações"**
4. Cadastrar requisitos gerais (se necessário)
5. Vincular requisitos ao contrato com peso (penalidade)

### 6.2 Avaliação (Aprovador)

1. Acessar uma visita técnica
2. Clicar em **"Aprovar"**
3. Na tela de aprovação, clicar em **"AVALIAR SERVIÇOS"**
4. Marcar os requisitos que **NÃO** foram cumpridos
5. Adicionar observações (opcional)
6. Clicar em **"Salvar Avaliação"**
7. Confirmar a aprovação

### 6.3 Dashboard (Gestor)

1. Acessar **Gestão > Avaliações**
2. Visualizar métricas gerais
3. Analisar contratos com mais requisitos
4. Identificar requisitos mais penalizados

---

## 7. Pendências para Implantação

### 7.1 Alta Prioridade

| # | Pendência | Descrição |
|---|-----------|-----------|
| 1 | Executar migração SQL | Rodar o arquivo `20260806_create_evaluation_system.sql` no banco de dados |
| 2 | Testar CRUD básico | Validar criação, edição e exclusão de requisitos |
| 3 | Testar vinculação | Validar vinculação de requisitos a contratos |
| 4 | Testar avaliação | Validar fluxo completo de avaliação na visita |

### 7.2 Média Prioridade

| # | Pendência | Descrição |
|---|-----------|-----------|
| 5 | Otimizar queries | Criar índices adicionais se necessário para performance |
| 6 | Adicionar permissões | Criar permissões de acesso para as novas telas |
| 7 | Testar dashboard | Validar carregamento e exibição das métricas |
| 8 | Integrar com notificações | Notificar prestador quando visita é avaliada |

### 7.3 Baixa Prioridade

| # | Pendência | Descrição |
|---|-----------|-----------|
| 9 | Métricas por prestador | Adicionar média de penalidades por empresa prestadora |
| 10 | Exportação PDF | Criar relatório de avaliações em PDF |
| 11 | Filtros por período | Adicionar filtro de data no dashboard |
| 12 | Gráficos detalhados | Adicionar gráficos de tendência temporal |

---

## 8. Testes

### 8.1 Testes Manuais

- [ ] Criar requisito geral
- [ ] Editar requisito geral
- [ ] Excluir requisito geral
- [ ] Vincular requisito ao contrato
- [ ] Editar peso do requisito no contrato
- [ ] Remover requisito do contrato
- [ ] Avaliar visita (marcar requisitos)
- [ ] Salvar avaliação
- [ ] Visualizar avaliação salva
- [ ] Acessar dashboard
- [ ] Verificar métricas

### 8.2 Cenários de borda

- [ ] Contrato sem requisitos vinculados
- [ ] Visita sem contrato associado
- [ ] Avaliação com todos os requisitos marcados
- [ ] Avaliação sem nenhum requisito marcado
- [ ] Edição de avaliação existente

---

## 9. Rollback

Caso seja necessário reverter a implantação:

```sql
-- Remover tabelas e views
DROP VIEW IF EXISTS public.v_orders_visits_evaluations;
DROP VIEW IF EXISTS public.v_contracts_evaluation_requirements;
DROP TABLE IF EXISTS public.orders_visits_evaluations;
DROP TABLE IF EXISTS public.contracts_evaluation_requirements;
DROP TABLE IF EXISTS public.cfg_evaluation_requirements;
```

---

## 10. Contato

Em caso de dúvidas ou problemas, entrar em contato com o responsável pelo desenvolvimento.

---

**Documento gerado automaticamente em 07/08/2026**
